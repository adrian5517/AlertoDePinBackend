# Environment Variables
NODE_ENV=development
PORT=5000

# MongoDB Connection
# Use a connection string without credentials in the repository. Example placeholder:
MONGODB_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/AlertoDePin

# JWT Secret
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Email Configuration (Optional - for notifications)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# Mapbox API Key (set this in your frontend `.env` as `VITE_MAPBOX_TOKEN`)
# MAPBOX_TOKEN=pk.YOUR_MAPBOX_TOKEN_HERE

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173

LOCATIONIQ_KEY=<your_locationiq_key_here>
